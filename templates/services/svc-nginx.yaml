apiVersion: v1
kind: Service
metadata:
  name: svc-nginx
  namespace: {{ .Release.name }}
spec:
  type: ClusterIP
  selector:
    app: nginx
  clusterIP: None
  ports:
  - port: 80
    protocol: TCP 
    targetPort: 80
